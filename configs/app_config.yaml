chunking:
  chunk_size: 800
  overlap: 120

retrieval:
  top_k: 5
  initial_k: 25
  entity_threshold: 0.45   # cosine similarity cutoff for semantic entity matching

embedding:
  model_name: sentence-transformers/all-MiniLM-L6-v2

output:
  include_sources: true
  include_assumptions: true
